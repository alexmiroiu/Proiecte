import classes from './Header.module.css';

const Header = () => {
    return <div className={classes.header}>
        <p>some logo here</p>
        <nav>some nav items here</nav>
    </div>
}

export default Header;