import classes from './options.module.css';

const Options = () => {

    return <div className={classes.optionsContainer}>
            <h2>Options</h2>
            <div className={classes.optionsWrapper}></div>
    </div>

}

export default Options